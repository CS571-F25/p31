import NotesBoard from "../components/NotesBoard";

function NotesPage() {
    return (
        <div>
            <h2>Sticky Notes Board</h2>
            <p>for random thoughts and tasks</p>
            <NotesBoard />
        </div>
    );
}

export default NotesPage